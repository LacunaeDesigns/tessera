{
  "job": "v0.3-booklet",
  "created": "2026-07-12T00:00:00Z",
  "notes": "Bound-booklet export (v0.3-booklet.md): turn a series into a home-bindable A5-on-A4 booklet. Big multi-turn feature; Task 3 (imposed render) + Task 4 (webfont) need an author physical-printer check (gated).",
  "chunks": [
    {"id": "b1-imposition", "status": "done", "commit": "ba40a6e", "notes": "DONE. TDD: tools/test-booklet.js (exact assertions, test-manifest idiom) FAIL -> js/booklet.js pure impose()/sequential() -> PASS. impose(8)=[{front:[8,1],back:[2,7]},{front:[6,3],back:[4,5]}]; impose(6) pads to 8 with nulls. Verify: node tools/test-booklet.js; all suites green. Idempotent: js/booklet.js exists with impose in api."},
    {"id": "b2-composer", "status": "done", "commit": "0fc89fb", "notes": "DONE. Registry 'Bind into a booklet' selection (letters with text only; textless disabled+reason), date order + up/down reorder (keyboard), compose logical pages (title / per-letter facts+letter / plates / colophon), paginate by measuring against the A5 content box. Verify: node --check; browser. BIG."},
    {"id": "b3a-sequential", "status": "done", "commit": "ed4f282", "notes": "DONE. printBooklet(model, rows, opts) in js/print.js: title/facts/letter(paginated)/plates/colophon/bind leaves, sequential two-up on A4 landscape (@page booklet in print.css), blank-leaf padding for odd totals. bk-print button wired in landing.js. Root-caused+fixed a real bug: pagination probe had no intrinsic size outside the .booklet-sheet flex context (maxH≈0, 3000+ leaf fragmentation) — pinned probe to 148.5mm x 210mm. Also fixed on-screen preview clipping (aspect-ratio card + fixed-pt fonts) by switching to auto-height like .sheet. Verified in-browser: 3-letter compose (9/16 leaves, byte-exact 7-leaf letter reconstruction), blank-padding path (7->8 leaves via direct TesseraPrint.printBooklet calls), no console errors, 375px no overflow. Gate 1 green. Idempotent: TesseraPrint.printBooklet in api; signature mode not yet implemented so re-running is safe."},
    {"id": "b3b-signature", "status": "in_progress", "commit": null, "notes": "SIGNATURE mode: impose() ordering (js/booklet.js, already fixture-tested), front/back page positioning left/right per the arrays, mode toggle in bk-preview UI (currently sequential-only, no picker). Duplex inset ±3mm: checked PROGRESS.md for real physical-walkthrough evidence to update the number from — none exists yet, so using the plan's placeholder ±3mm as-is (provisional, pending the author's physical check which is the gate for this whole task anyway). Threading a `duplex` flag through the shared leaf builders (not just CSS-cascading it) so the pagination probe and final render always share identical box geometry — a mode-only CSS override would silently mismatch the two and risk overflow. Single-signature (<=5 sheets) guidance goes on the bind leaf itself (computed post-pagination, exact count in hand) rather than a pre-print estimate (pagination is async/expensive, not available at preview time). Pamphlet-stitch SVG + scissors-free numbered steps on a new bkBindLeafSignature (sequential's bkBindLeafSequential, cut-and-staple, untouched). Verify impose-driven page order + duplex class presence in DOM, Gate 1 green, no console errors. Physical duplex fold-check = AUTHOR GATE (out of scope for this chunk)."},
    {"id": "b4-qa", "status": "pending", "commit": null, "notes": "/qa PDF both modes + browser; PROGRESS/plan/vault docs (first PROGRESS.md touch for this feature — b1-b3a didn't touch it, matching precedent). Physical duplex fold-check is author-gated. Task 4 (webfont decision) is author-gated on print evidence — deferred, noted."}
  ],
  "resume_hint": "b3b-signature in progress: add impose()-driven duplex mode to printBooklet (js/print.js), thread a duplex flag through leaf builders for pagination-safe ±3mm inset, add mode toggle to bk-preview UI (index.html + landing.js). Then b4-qa. Task 3 physical fold-check + Task 4 webfont are author-gated."
}
