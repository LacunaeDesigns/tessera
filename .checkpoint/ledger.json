{
  "job": "v0.3-booklet",
  "created": "2026-07-12T00:00:00Z",
  "notes": "Bound-booklet export (v0.3-booklet.md): turn a series into a home-bindable A5-on-A4 booklet. Big multi-turn feature; Task 3 (imposed render) + Task 4 (webfont) need an author physical-printer check (gated).",
  "chunks": [
    {"id": "b1-imposition", "status": "done", "commit": "ba40a6e", "notes": "DONE. TDD: tools/test-booklet.js (exact assertions, test-manifest idiom) FAIL -> js/booklet.js pure impose()/sequential() -> PASS. impose(8)=[{front:[8,1],back:[2,7]},{front:[6,3],back:[4,5]}]; impose(6) pads to 8 with nulls. Verify: node tools/test-booklet.js; all suites green. Idempotent: js/booklet.js exists with impose in api."},
    {"id": "b2-composer", "status": "done", "commit": "0fc89fb", "notes": "DONE. Registry 'Bind into a booklet' selection (letters with text only; textless disabled+reason), date order + up/down reorder (keyboard), compose logical pages (title / per-letter facts+letter / plates / colophon), paginate by measuring against the A5 content box. Verify: node --check; browser. BIG."},
    {"id": "b3a-sequential", "status": "done", "commit": "ed4f282", "notes": "DONE. printBooklet(model, rows, opts) in js/print.js: title/facts/letter(paginated)/plates/colophon/bind leaves, sequential two-up on A4 landscape (@page booklet in print.css), blank-leaf padding for odd totals. bk-print button wired in landing.js. Root-caused+fixed a real bug: pagination probe had no intrinsic size outside the .booklet-sheet flex context (maxH≈0, 3000+ leaf fragmentation) — pinned probe to 148.5mm x 210mm. Also fixed on-screen preview clipping (aspect-ratio card + fixed-pt fonts) by switching to auto-height like .sheet. Verified in-browser: 3-letter compose (9/16 leaves, byte-exact 7-leaf letter reconstruction), blank-padding path (7->8 leaves via direct TesseraPrint.printBooklet calls), no console errors, 375px no overflow. Gate 1 green. Idempotent: TesseraPrint.printBooklet in api; signature mode not yet implemented so re-running is safe."},
    {"id": "b3b-signature", "status": "done", "commit": "af59615", "notes": "DONE. Signature mode in printBooklet(): TesseraBooklet.impose() orders pages onto duplex A4 sheets (front then back per physical sheet), bk-cut--fold divider (not cut), bkBindLeafSignature (pamphlet-stitch SVG, scissors-free numbered steps, sheet-count note w/ >5-sheet volume-split warning). `duplex` flag threaded through every leaf builder incl. the pagination probe so ±3mm duplex inset (bk-leaf--duplex, +3mm all sides — no physical evidence in PROGRESS.md yet to refine from, plan's placeholder kept) never desyncs measurement from render. Mode picker added to bk-preview (index.html/landing.css/landing.js), sequential still default. Fixed real wiring gap found along the way: js/booklet.js was never <script>-included in index.html or in sw.js CORE — TesseraBooklet didn't exist in-browser until this commit. Verified in-browser: signature output for 5 letters (20 leaves/5 sheets) checked position-by-position against a fresh impose(20) call, exact match every position; 5-sheet vs 6-sheet warning boundary both correct; sequential mode regression-checked post-refactor (0 duplex leaves, cut not fold); no console errors; 375px picker no overflow. Gate 1 green. Idempotent: mode picker defaults sequential, signature is additive."},
    {"id": "b4-qa", "status": "pending", "commit": null, "notes": "/qa PDF both modes + browser; PROGRESS/plan/vault docs (first PROGRESS.md touch for this feature — b1-b3b didn't touch it, matching precedent). Physical duplex fold-check is author-gated (both modes now fully implemented and ready for the physical walkthrough). Task 4 (webfont decision) is author-gated on print evidence — deferred, noted."}
  ],
  "resume_hint": "Start b4-qa: run /qa (PDF both modes + browser), then update docs/PROGRESS.md + the v0.3-booklet plan + Ariadne vault to record the whole booklet feature (Tasks 1-3) done. Physical duplex fold-check is the author's gate — flag it clearly rather than attempting to fake/skip it. Task 4 (webfont) stays deferred, author-gated on print evidence."
}
